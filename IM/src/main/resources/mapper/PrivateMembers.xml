<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.com.code.im.mapper.PrivateMemberMapper">
    <insert id="insertPrivateMember" parameterType="PrivateMembers">
        insert into private_members(sessionId,userId1,userId2)
        values(#{sessionId},#{userId1},#{userId2})
    </insert>
</mapper>
